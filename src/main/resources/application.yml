server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:mysql://1.12.248.106:3308/demo22?characterEncoding=utf-8&useSSL=false
    username: root
    password: Hex.863731274
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    # 显示sql语句
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        # 格式化sql语句
        format_sql: true
        multiTenancy: DATABASE
        tenant_identifier_resolver: com.luckykuang.auth.config.TenantIdentifierResolver

logging:
  level:
    # 用于查看传入的参数
    # 基本类型绑定
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    # 枚举类型绑定
    org.hibernate.metamodel.model.convert.internal.NamedEnumValueConverter: trace

## App Properties
app:
  jwt-secret: JWTSecretKey
  jwt-expiration-milliseconds: 604800000

# springdoc配置
springdoc:
  api-docs:
    # true - 开启API文档功能，false - 关闭API文档功能
    enabled: true
  swagger-ui:
    # “list”（仅展开标记）、“full”（展开标记和操作）、“none”（不展开任何内容）。
    docExpansion: none